import { UI } from "@/components/UI";
import { AvatarCanvas } from "@/components/AvatarCanvas";

const App = () => {
  return (
    <>
      <UI />
      <AvatarCanvas />
    </>
  );
};

export default App;
